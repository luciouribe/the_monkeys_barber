export const getterUserName = (state, getters) => {
    return state.userName;
}

export const getterUserEmail = (state, getters) => {
    return state.userEmail;
}

export const getterAccessToken = (state, getters) => {
    return state.accessToken;
};