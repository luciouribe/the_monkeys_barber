import { boot } from "quasar/wrappers";

export default boot(async ({ store }) => {
  // store
  //   .dispatch("permisos/getPermissions")
  //   .then((res) => {
  //     store.commit("permisos/setPermissions", res.data);
  //   })
  //   .catch((error) => {
  //     console.error(error);
  //   });
});
